# 负向触发示例

以下提示词必须不触发 pvm-env-manager skill：

1. "帮我做 git 版本回滚" — 这是 git 操作，不是运行时版本管理
2. "帮我配置 Docker 容器环境" — 这是容器环境，不是运行时版本
3. "用 apt 安装 nginx" — 这是系统包管理，不是运行时版本
4. "帮我开发 pvm 的安装器功能" — 这是开发 PVM 源码，不是使用 PVM
5. "我的 npm 包版本冲突了" — 这是包依赖问题，不是运行时版本
6. "帮我配置环境变量 JAVA_HOME" — 这是 Java 环境配置，PVM 不管理 Java（目前）
