---
alwaysApply: true
description: PVM 环境管理强制规则
globs: "**/*"
---

# PVM 环境管理规则

## 铁律

1. **先检测后操作** — 任何 pvm 操作前先 `pvm --version`，未装先装
2. **先装后卸** — 卸 nvm 前先 `pvm install node`，卸 pyenv 前先 `pvm install python`
3. **项目级优先** — 切版本优先 `pvm use --local`（写 .pvmrc），而非全局
4. **冲突必清** — 检测到 nvm/fnm/pyenv 等必须处理，不能忽略
5. **脚本优先** — 卸载/诊断/配置有现成脚本，直接调不手写
6. **少废话** — 直接执行命令，报关键结果，不解释原理
7. **跨平台** — Windows 用 PowerShell，macOS/Linux 用 bash，不混用

## 禁止

- ✗ 未检测 pvm 就跑 `pvm use`
- ✗ 卸 nvm 前没装 node
- ✗ 跳过检测直接安装
- ✗ Windows 上用 bash 语法 / macOS 上用 PowerShell

## 命令速查

| 操作 | 命令 |
|------|------|
| 检测 | `pvm --version` |
| 安装运行时 | `pvm install node@20` |
| 切换（用户级） | `pvm use node@20` |
| 锁定（项目级） | `pvm use node@20 --local` |
| 已装列表 | `pvm list` |
| 可用版本 | `pvm list node` |
| 当前版本 | `pvm current` |
| 诊断 | `pvm doctor` |
| 修复 | `pvm setup` |
| 更新 | `pvm self-update` |
| 卸载运行时 | `pvm remove node@20` |
| 卸载 PVM | `pvm uninstall` |

## 支持的运行时（9种）

node / python / rust / go / bun / deno / git / pnpm / yarn

**全局唯一（不支持项目级 .pvmrc）：go, git, rust**

## 冲突工具检测

pvm doctor 自动检测：nvm, volta, fnm, **nodenv**, pyenv, asdf, **conda/anaconda/miniconda**

## 脚本速查（冲突/诊断直接调）

```
目录: .codebuddy/skills/pvm-env-manager/scripts/

卸载软件    uninstall-tool.bat/.sh <名称> --yes   (18种: PVM运行时 + nvm/volta/fnm/nodenv/pyenv/rustup/asdf/conda)
pnpm锁版本  setup-npmrc.bat/.sh --force
环境诊断    diagnose-pvm-env.bat/.sh --fix
```

## 安装源

- GitHub: https://github.com/Lycorxy/pvm
- Releases: https://github.com/Lycorxy/pvm/releases
- 策略: A 一键脚本(可能404) → B Releases下载 → C 源码编译，失败自动降级
