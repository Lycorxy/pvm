# .pvmrc 模板
# 放在项目根目录，提交到 Git 仓库
# 团队成员 clone 后 cd 进目录，版本自动生效

# ── JavaScript 生态（推荐组合）──
node = 20.11.0
pnpm = 9

# 其他可选运行时（按需取消注释）
# yarn = 1.22
# bun = 1.1.0
# deno = 1.41

# ── Python ──
# python = 3.12.0

# ── 以下仅支持用户级切换，不支持项目级 ──
# go = 1.22.0
# rust = 1.78.0
# git = 2.55.0
